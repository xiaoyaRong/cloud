server:
  port: 7001

eureka:
  instance:
    hostname: localhost #服务端事例名称
  client:
    register-with-eureka: false #不需要向注册中心注册自己
    fetch-registry: false #表示自己是注册中心
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/ #注册中心地址